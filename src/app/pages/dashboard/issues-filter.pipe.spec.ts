import { IssuesFilterPipe } from './issues-filter.pipe';

describe('IssuesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new IssuesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
